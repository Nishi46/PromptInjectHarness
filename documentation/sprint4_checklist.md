# Sprint 4 Checklist — Adaptive Attacks (E?)

Granular sub-steps for each task in Sprint 4 of [sprint_planning.md](sprint_planning.md). Check items off as you go; each should produce a concrete artifact (a file, a passing test, a committed config/result). Ordered to respect the `Deps` column in the sprint table — work top to bottom. Grounded in the Sprint 1–3 codebase (`adapters/agentdojo_adapter.py::run_episode`, `adapters/mcp_adapter.py::run_mcp_episode`, `attacks/registry.py`, `attacks/families/*.py`, `mcp/poisoned.py::PoisonedCase`, `trace/db.py` + `trace/schema.sql`, `trace/queries.py`, `sweep/runner.py`, `clients/base.py::ModelClient`) — reread those before starting. Also reread [docs/notes/adaptive_attacks.md](../docs/notes/adaptive_attacks.md) in full: it already documents where this sprint's design (S4-01's discrete feedback, S4-02's LLM mutation, S4-03's per-task round budget) diverges from the Zhan et al. paper it cites. That divergence is real and should be stated plainly in S4-08's writeup, not glossed over.

> **Read this before writing any code.** Every existing execution path runs exactly one static, pre-computed injection payload per episode: `agentdojo_adapter.run_episode` calls `attack.attack(user_task, injection_task)` once and hands the resulting `injections: dict[str, str]` straight to `suite.run_task_with_pipeline`; `mcp_adapter.run_mcp_episode` calls `PoisonedCase.apply(server)` once using the case's fixed `injection_text`. Sprint 4 needs to re-run the *same* (defense, task) pair up to 20 times with a *different* payload each round. Three of the five AgentDojo families (`naive`, `ignore_previous`, `important_instructions`) build their payload from a `FixedJailbreakAttack.jailbreak` template string — but `encoding_obfuscation` computes its payload in Python code (`base64.b64encode(...)`, see `attacks/families/encoding_obfuscation.py`), with no template string to swap. That rules out "mutate the attack object's internal state" as a uniform hook. **The design used throughout this checklist: mutate the *output* of round 1's payload computation, not the attack machinery that produced it**, and plumb a plain override string/dict into `run_episode`/`run_mcp_episode` for every round after the first. This keeps the mutator suite- and family-agnostic (it only ever sees "the current payload text" and "the injection goal", both plain strings) and needs exactly one small, additive parameter on each existing adapter entrypoint — not a rewrite of either. Write this decision down explicitly as the first S4-01 step so nothing downstream re-derives it.

---

## S4-01 — Feedback channel (2h) 🔴 — deps: S2-08 — done

- [x] Append an "## Implementation decisions for this project's Sprint 4" section to `docs/notes/adaptive_attacks.md` recording the architecture decision from the note above (mutate-the-output, not the attack internals) and why (`encoding_obfuscation` has no template string). This is the load-bearing decision every later S4 task assumes.
- [x] Create the `src/injection_pareto/adaptive/` package (`__init__.py`, empty).
- [x] In a new `src/injection_pareto/adaptive/feedback.py`, define `@dataclass EpisodeFeedback`: `success: bool`, `defense_intervened: bool`, `refusal_text: str | None` — exactly the three signals S4-01 names, no more.
- [x] Write `extract_feedback(conn: sqlite3.Connection, *, episode_id: int) -> EpisodeFeedback`:
  - `success` reads `episode.security` for that row (raise `ValueError` if the row's `injection_task_id` is `NULL` — this function is only ever called on an attacked episode inside the adaptive loop).
  - `defense_intervened` is `True` iff any `defense_event` row for the episode has `verdict = 'block'` (reuse `trace/queries.py::reconstruct_episode` rather than a new raw query, to stay consistent with how `scoring/security.py` already reads a trace).
  - `refusal_text` is the `content` of the last `step` row with `role = 'assistant'` and non-empty `content`, truncated to a fixed cap (e.g. 500 chars — this only ever feeds a mutation prompt, not a replay); `None` if no assistant step has any text.
- [x] Unit tests in `tests/test_adaptive_feedback.py` (8 tests), built against a real trace DB fixture (insert rows directly via `trace/db.py` helpers, no live model call):
  - full compromise (`episode.security = 1`) → `success=True`.
  - a `defense_event` row with `verdict='block'` present, `episode.security = 0` → `defense_intervened=True`, `success=False` (both a step-level and an episode-level block, as two separate tests).
  - no block, `security=0`, one assistant step with text → `refusal_text` populated and truncated correctly at the cap boundary; a second test confirms an empty-content assistant step doesn't overwrite an earlier real one.
  - no assistant steps at all → `refusal_text is None`.
  - calling `extract_feedback` on a benign episode (`injection_task_id IS NULL`) raises `ValueError`; an unknown `episode_id` also raises `ValueError`.
- [x] `ruff check .`, `mypy .`, full `pytest` suite (242 tests) all clean.

## S4-02 — Mutation engine (4h) 🔴 — deps: S4-01 — done

- [x] Exposed a public `ATTACK_FAMILIES` tuple on `attacks/registry.py` (`tuple(_FAMILY_TO_AGENTDOJO_NAME)`, re-exported from `attacks/__init__.py`) — needed so the mutator's family list can be checked against the real registry instead of a second hardcoded copy.
- [x] In a new `src/injection_pareto/adaptive/mutator.py`, defined `FAMILY_CONSTRAINTS: dict[str, str]` with one entry per attack family this sprint covers — the 5 AgentDojo families plus the 4 MCP sub-families (imported from `attacks.registry.ATTACK_FAMILIES` / `mcp.poisoned.SUB_FAMILIES`, not re-typed) — each a one-sentence invariant a mutation must preserve. A module-level check raises `ValueError` immediately at import time if `FAMILY_CONSTRAINTS`'s keys ever drift from the union of both registries (same "fail at import on a typo" pattern `mcp/injection_tasks.py` already uses).
- [x] Wrote `_build_mutation_messages(*, family, current_payload, goal, feedback, round_index) -> list[Message]` — system message states the family constraint + "respond with ONLY the rewritten payload"; user message gives the current payload, the goal, and the two observable feedback signals (`defense_intervened`, `refusal_text`) as plain text.
- [x] Wrote `mutate_payload(model_client, *, family, current_payload, goal, feedback, round_index, responses=None) -> str` — calls `model_client.generate(ModelRequest(messages=...))`, strips wrapping quotes/code fences via `_clean_response_text`, returns it.
- [x] Wrote `_looks_in_family(family, payload) -> bool` — a documented **heuristic** keyed off `_FAMILY_MARKERS` (per-family keyword/substring lists; `naive` passes iff *none* of every other family's markers appear, since "no jailbreak framing" is its whole definition). Docstring states plainly this is a cheap heuristic, not a classifier.
- [x] Wired the retry-once-on-drift behavior inside `mutate_payload`: a first response failing `_looks_in_family` triggers one corrective re-prompt naming the family and constraint; if the retry also drifts, returns `current_payload` unchanged — never raises, never silently accepts an out-of-family payload.
- [x] Cost accounting: `mutate_payload` appends every `ModelResponse` it produces (including the retry) to a caller-supplied `responses: list[ModelResponse] | None` — S4-03's trial loop will fold these into `cost_record` rows labeled `mutator:<family>`.
- [x] Unit tests in `tests/test_adaptive_mutator.py` (27 tests) with a scripted fake `ModelClient`: `FAMILY_CONSTRAINTS` keys exactly match `{5 AgentDojo families} | {4 MCP sub-families}` and all 9 constraint strings are distinct; `_looks_in_family` parametrized over one in-family/one out-of-family payload per all 9 families, plus an empty-payload case; happy path (in-family first response, verbatim after cleaning); code-fence/quote stripping; retry-on-drift then accept; fallback-to-`current_payload` when both attempts drift; every response (including retries) lands in the `responses` accumulator; unknown family raises `ValueError` before any model call; the prompt text carries both feedback signals and the goal.
- [x] `ruff check .`, `mypy .`, full `pytest` suite (269 tests) all clean.

## S4-03 — Budget controller (2h) 🔴 — deps: S4-02 — done

- [x] Added `ADAPTIVE_ROUND_BUDGET = 20` as a module-level constant in a new `src/injection_pareto/adaptive/trial.py` — the single source of truth for "N=20 rounds", never a per-call parameter that could silently vary between defenses/attacks. (The function still *accepts* an override `budget` kwarg, deliberately — see the note below on where the "identical for every pair" guarantee actually lives.)
- [x] Extended `trace/schema.sql` with two new tables + indices: `adaptive_trial(id, run_id REFERENCES run(id), task_id, defense, attack_family, suite, budget NOT NULL, success, rounds_to_success, started_at NOT NULL, ended_at)` and `adaptive_round(id, trial_id REFERENCES adaptive_trial(id), round_index NOT NULL, episode_id NOT NULL REFERENCES episode(id), payload_text NOT NULL, timestamp NOT NULL)`, plus `idx_adaptive_trial_run_id`/`idx_adaptive_round_trial_id`.
- [x] Added `trace/db.py` helpers, following the existing commit conventions exactly: `insert_adaptive_trial(...)` (commits, mirrors `insert_run`), `insert_adaptive_round(...)` (does not commit — for use inside `transaction()`), `update_adaptive_trial_result(...)` (commits, mirrors `update_episode_partial_compromise`). Re-exported from `trace/__init__.py`.
- [x] Regression tests in a new `tests/test_adaptive_trace_schema.py` (4 tests): both tables exist after `init_db`; `insert_adaptive_trial` → `insert_adaptive_round` (×2) → `update_adaptive_trial_result` round-trips correctly; FK from `adaptive_round.trial_id` and from `adaptive_round.episode_id` are each independently enforced (`PRAGMA foreign_keys = ON`).
- [x] Defined `@dataclass AdaptiveTrialResult` in `trial.py`: `trial_id`, `rounds_run`, `success`, `rounds_to_success`.
- [x] Added the small, additive override parameters the preamble's architecture decision requires:
  - `agentdojo_adapter.run_episode`: new optional `injections_override: dict[str, str] | None = None`. When set, skips `load_attack(...).attack(...)` entirely and uses the override dict as-is (`injection_task` is still resolved either way, since only `attack.attack()` is skipped).
  - `mcp_adapter.run_mcp_episode`: new optional `injection_text_override: str | None = None`. When set, uses `dataclasses.replace(case, injection_text=injection_text_override)` (`replace` was already imported) in place of the looked-up `case` before `.apply()`.
  - Unit tests for both: `tests/test_agentdojo_adapter.py` gained 2 tests (real `workspace` suite, `suite.run_task_with_pipeline` monkeypatched to capture the `injections` dict without a live model call — proves `load_attack` is skipped when the override is set, and that omitting it exactly reproduces the real `attack.attack()` output); `tests/test_mcp_adapter.py` gained 2 tests (real S3-02 `file_storage` server + case, asserting the captured tool schema's description contains the override text and not the case's own `injection_text`, and the reverse when omitted).
- [x] Wrote `run_adaptive_trial(...)` in `trial.py`. Scoped to S4-03 only: the loop **always runs all `budget` rounds**, no early exit (S4-04 adds that):
  - inserts the `adaptive_trial` row up front with `budget=budget`.
  - round 1's payload = `base_payload` unchanged, passed through the *same* override mechanism every later round uses (not a different code path) — see the architecture note.
  - each round dispatches to `run_episode_fn` (non-`mcp` suites, broadcasting the payload to every entry of a required `injection_points` tuple) or `run_mcp_episode_fn` (`suite == "mcp"`, a bare string), inserts the `adaptive_round` row, calls `extract_feedback_fn`, and — if not the last round — computes the next payload via `mutate_payload_fn`.
  - every `ModelResponse` the mutator produced is written to `cost_record` labeled `mutator:<attack_family>`, attributed to the round whose feedback triggered that mutation.
  - a **fresh `DefenseStack`** is built via a required `defense_stack_factory` callable for every round — reusing one across rounds would double-count a stateful defense's own `.responses` into `cost_record` on every subsequent round's trace write (documented explicitly in the docstring; not merely implied).
  - after the loop: `update_adaptive_trial_result(success = any round succeeded, rounds_to_success = None)` — S4-04 fills in the real value.
- [x] Multi-injection-point simplification stated explicitly in the docstring: broadcasting one mutated string to every entry of `injection_points` mirrors `FixedJailbreakAttack.attack()`'s own existing behavior.
- [x] Unit tests in `tests/test_adaptive_trial.py` (9 tests) with fakes (no real model/AgentDojo calls, though the fakes insert real `episode` rows so the FK holds): never-succeeding trial runs exactly `ADAPTIVE_ROUND_BUDGET` rounds/rows and never mutates after the last round; a mid-trial success still runs every remaining round (S4-03's explicit non-goal, confirming S4-04 hasn't accidentally landed early); an explicit different `budget` is honored and recorded as-is; mutator cost lands in `cost_record` labeled `mutator:<family>`; the override payload is broadcast identically to multiple `injection_points`; `suite == "mcp"` dispatches to `run_mcp_episode_fn` with a bare string; missing `run_episode_fn`/`injection_points`/`run_mcp_episode_fn` each raise a clear `ValueError`.
- [x] Documented the one known limitation plainly in `trial.py`'s module docstring: unlike `sweep/runner.py`'s per-episode resumability, a trial interrupted mid-loop is **not** resumed round-by-round — re-running it starts over from round 1. Acceptable at S4-05's cost-capped scale.
- [x] `ruff check .`, `mypy .`, full `pytest` suite (286 tests) all clean.

## S4-04 — Early-stop + `rounds_to_success` (1h) 🟡 — deps: S4-03

- [ ] In `run_adaptive_trial`'s loop, add the early-stop branch: after computing each round's `EpisodeFeedback`, if `feedback.success` is `True`, stop iterating immediately — do not run remaining rounds, do not compute a next mutation.
- [ ] Compute `rounds_to_success`: the 1-indexed round number of the first successful round, or `None` if the loop exhausted `budget` rounds without success (explicitly distinct from "ran 20 rounds", queryable via `IS NULL`).
- [ ] Update `AdaptiveTrialResult`/`update_adaptive_trial_result` call site to pass the real `rounds_to_success` and `success` values (replacing S4-03's temporary `None`/loop-through-all-rounds placeholder).
- [ ] Docstring note on `rounds_to_success`, quoting the sprint plan directly: "a richer signal than binary ASR" — a defense broken on round 2 is meaningfully weaker than one broken on round 19, even though both count as `success=True` under ASR@20.
- [ ] Unit tests added to `tests/test_adaptive_trial.py`: a fake feedback sequence that succeeds on round 7 → loop stops after round 7, exactly 7 `adaptive_round` rows exist, `rounds_to_success == 7`, `mutate_payload_fn` was called exactly 6 times (never called for a round that won't run); a fake sequence that never succeeds → 20 rounds run, `rounds_to_success is None`; a fake sequence that succeeds on round 1 → exactly 1 `adaptive_round` row, `mutate_payload_fn` never called at all.
- [ ] `ruff check .`, `mypy .`, `pytest tests/test_adaptive_trial.py` clean.

## S4-05 — Run adaptive sweep: 6 defenses × 5 families × 2 models, cost-capped (4h + wall) 🔴 — deps: S4-03

- [ ] Write `src/injection_pareto/adaptive/sweep.py::run_adaptive_sweep(config: ExperimentConfig, *, concurrency=4, provider_concurrency=None, no_cache=False, cache_dir=..., show_progress=True, sample_fraction: float | None = None, run_adaptive_trial_fn=run_adaptive_trial) -> AdaptiveSweepSummary` — deliberately mirrors `sweep/runner.py::run_sweep`'s shape (`expand_run_specs`, `ThreadPoolExecutor`, per-provider `Semaphore`, a `_Point`-equivalent, an injectable `*_fn` for testability) but its unit of work is one **trial** `(model, defense, attack_family, task)`, not one episode.
- [ ] Add `_AdaptiveSweepFailure`/`AdaptiveSweepSummary` dataclasses paralleling `SweepFailure`/`SweepSummary` — `total_trials`, `completed`, `failed`, `failures`.
- [ ] Add the `sample_fraction` parameter: when set (e.g. `0.01`), deterministically subsample the expanded trial-point list (e.g. every Nth point by a fixed seed) instead of running the full grid — this is the mechanism S4-05's "run a 1% sample and extrapolate the bill" cost-risk mitigation actually executes against, not a separate ad hoc script.
- [ ] Add an `adaptive-run` subcommand to `src/injection_pareto/__main__.py`, parallel to the existing `run` subcommand: same `--concurrency`/`--no-cache`/`--no-progress` flags plus `--sample-fraction FLOAT`; prints total trial count and a per-trial round budget reminder before starting (so a human sees the multiplier before committing $).
- [ ] Write `configs/adaptive_sweep.yaml`: all 6 defenses (`no_defense` included — it's the reference point ASR@1/ASR@20 need), the 5 AgentDojo attack families, the 2-model set reused from S2-11/S3-06's validated pair (`llama3.2:3b`, `llama3.1:latest`), and a **small** task list — deliberately smaller than S1/S2's full task set, sized so `6 × 5 × 2 × |tasks| × 20 rounds` stays wall-clock-reasonable; write the actual episode-count arithmetic in a comment at the top of the file so the multiplier is visible before running it.
- [ ] Validate the config loads and expands cleanly (`load_config` + `expand_run_specs`) before running anything, same as every prior sprint's config.
- [ ] Run `adaptive-run --sample-fraction 0.01` for real against `configs/adaptive_sweep.yaml`; record the observed $ and wall-clock in `docs/notes/adaptive_attacks.md`'s new implementation-decisions section, and extrapolate to the full-grid estimate before proceeding — this is a real go/no-go checkpoint, not a formality.
- [ ] Run the full sweep for real (or a deliberately reduced, documented subset if the extrapolation forces a cut — cut task count before cutting model count, per Appendix A.5's stated priority); confirm 0 failures or triage real failures the way S3-06 did (root-cause, don't just retry).
- [ ] Unit tests in `tests/test_adaptive_sweep.py`, no real model calls: a fake `run_adaptive_trial_fn` proves dispatch/concurrency/provider-semaphore wiring (mirrors `tests/test_sweep_runner.py`'s existing pattern); `sample_fraction=0.5` on a small fake grid returns half the trials deterministically (same subsample on repeated calls); `sample_fraction=None` runs every point.
- [ ] `ruff check .`, `mypy .`, full `pytest` clean.

## S4-06 — Run adaptive sweep on the MCP suite (2h + wall) 🟡 — deps: S4-05, S3-06

- [ ] Extend `adaptive/sweep.py`'s per-point dispatch with the same `suite == "mcp"` branch `sweep/runner.py::_run_point` already uses — route to `run_mcp_episode_fn` (via `run_adaptive_trial`'s existing `run_mcp_episode_fn` parameter from S4-03) instead of `run_episode_fn`.
- [ ] Confirm `run_adaptive_trial`'s round-1 `base_payload`/`goal` inputs resolve correctly for an MCP point: `base_payload = get_case(poisoned_case_id).injection_text`, `goal = get_case(poisoned_case_id).injection_goal`, `attack_family = case.sub_family` (one of the 4 `SUB_FAMILIES`, matching an S4-02 `FAMILY_CONSTRAINTS` key already).
- [ ] Write `configs/adaptive_mcp_sweep.yaml`: 6 defenses × 4 sub-families × the same 2-model set as S4-05, `mcp_tasks`/`mcp_poisoned_cases` filtered exactly the way `config/loader.py::expand_run_specs`'s existing `suite == "mcp"` branch already filters for S3-06 (no new filtering logic needed — reused as-is).
- [ ] Run the sweep for real; confirm 0 failures.
- [ ] Add an MCP-suite dispatch regression test to `tests/test_adaptive_sweep.py`, mirroring `tests/test_sweep_runner.py`'s existing "mcp routes to the mcp fn, never the agentdojo fn" test.
- [ ] `ruff check .`, `mypy .`, full `pytest` clean.

## S4-07 — Reordering analysis: ASR@1 vs ASR@20, rank correlation (2.5h) 🔴 — deps: S4-05

- [ ] Write `scripts/generate_adaptive_results.py`, mirroring `scripts/generate_static_baseline.py`/`scripts/generate_mcp_suite_results.py`'s existing pattern (typed row dataclasses + one query function each, deterministic output, no new dependency).
- [ ] `_asr_at_1` query/table: per defense (and per attack family), the success rate of `adaptive_round` rows where `round_index = 1`, joined to `episode.security` — note in a comment that this is mathematically the same population as the existing static-baseline ASR (S1/S2), a cross-check worth actually running once the numbers exist.
- [ ] `_asr_at_20` query/table: per defense (and per attack family), `mean(adaptive_trial.success)` over all trials for that (defense, family) pair.
- [ ] `_rounds_to_success_distribution` query/table: per defense, count/median/mean of `adaptive_trial.rounds_to_success` among trials where it's not `NULL`, plus the count that never succeeded within budget.
- [ ] Implement Spearman rank correlation by hand (no new dependency — matches `trace/queries.py::_percentile`'s existing hand-rolled-over-scipy precedent): rank defenses by `ASR@1` and separately by `ASR@20`, compute `rho` over the two rank vectors.
- [ ] Write `results/adaptive.md`: the three tables above, the rank-correlation number, and an explicit, honest statement of whether any defense's rank changed materially between the two orderings — including the "none did" outcome if that's what the data shows, per the acceptance criterion's own wording.
- [ ] Reproducibility check (matches S3-07's pattern exactly): run the script twice against the same trace DB, diff output with the timestamp line excluded — byte-identical.
- [ ] `ruff check .`, `mypy .` clean.

## S4-08 — Qualitative writeup: what the successful mutation exploited (2h) 🟡 — deps: S4-07

- [ ] For every defense that "broke" (materially higher `ASR@20` than `ASR@1` for at least one attack family), find its winning trial(s) — `adaptive_trial.success = 1` — and pull the `adaptive_round.payload_text` sequence from round 1 through the success round for that trial.
- [ ] Add a "Qualitative analysis" section to `results/adaptive.md`: 2-4 sentences per broken defense, naming the *specific textual mechanism* the winning mutation introduced relative to round 1's payload (e.g. dropped a literally-pattern-matched phrase, added a framing the defense's prompt/classifier wasn't trained to catch, switched encoding) — read the actual round-1-vs-winning-round diff before writing this, don't speculate.
- [ ] Explicitly restate, in this section, the divergences from Zhan et al. already recorded in `docs/notes/adaptive_attacks.md` (S4-01's feedback channel and S4-02's LLM-driven mutation have no precedent in that paper's white-box gradient-optimization method; S4-07's rank-correlation analysis is this project's own contribution, not a reproduction) — so the writeup cites the paper as motivation without overclaiming it as the mechanism being reproduced.
- [ ] If a defense did **not** break (ASR@20 stayed at or near ASR@1), say so plainly rather than omitting it — a defense that held under 20 rounds of LLM-driven adaptive mutation is itself a real, reportable result (matches this project's existing pattern of reporting honest null results in `results/static_baseline.md`/`results/mcp_suite.md`).
- [ ] Full `pytest` suite, `ruff check .`, `mypy .` all clean after the change.
- [ ] Commit left to the user, per this project's standing instruction not to run `git commit` unless explicitly asked.

---

## Acceptance criteria (from sprint_planning.md)

- [ ] Every (defense, attack) pair received exactly the same refinement budget — `ADAPTIVE_ROUND_BUDGET = 20` is a single module constant every `configs/adaptive*.yaml` sweep runs against unmodified; asserted by an S4-03 test, stated in the README.
- [ ] `results/adaptive.md` reports ASR@1, ASR@20, and the rounds-to-success distribution.
- [ ] At least one defense's rank changes materially between static and adaptive — and if none does, that's a real finding too; report it honestly (S4-07/S4-08).

**Cost risk — the big one.** 6 defenses × 5 attacks × 20 rounds × tasks × models multiplies fast. `adaptive-run --sample-fraction 0.01` (S4-05) exists specifically to run a 1% sample and extrapolate the bill *before* launching the full S4-05 sweep. Cut models before you cut rounds; rounds are the scientific claim.

**Watch for:**
- The `encoding_obfuscation` family has no template string to mutate in place — this is why every override is plumbed as a post-hoc string/dict into `run_episode`/`run_mcp_episode`, not as a mutation of attack-object internals (see the preamble). Don't special-case this family; the uniform override design should already cover it.
- A trial is not resumable mid-loop the way `sweep/runner.py`'s episodes are (S4-03) — a process restart re-runs a trial from round 1. Fine at S4-05's cost-capped scale; would need revisiting before scaling this up.
- `_looks_in_family` (S4-02) is a cheap heuristic, not a classifier — it exists only to catch obviously-drifted mutations and fall back safely, not to guarantee every accepted mutation is genuinely in-family. Don't lean on it for anything scored.
- Multi-injection-point AgentDojo tasks get one mutated payload applied identically to every candidate (mirrors `FixedJailbreakAttack`'s own existing behavior) — verify against the real S4-05 task list whether this ever actually matters before assuming it's a non-issue.
